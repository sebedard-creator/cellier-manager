{
  "name": "cellier-manager-extension",
  "private": true,
  "type": "module",
  "version": "0.6.8",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc && node scripts/build.mjs",
    "test": "npm run build && node --test test/*.test.mjs"
  },
  "devDependencies": {
    "@types/chrome": "0.0.287",
    "typescript": "5.7.2"
  }
}
